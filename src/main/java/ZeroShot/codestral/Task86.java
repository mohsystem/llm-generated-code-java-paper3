package ZeroShot.codestral;
// Java doesn't provide a built-in way to execute downloaded EXE files.
// Java's standard libraries don't allow executing native executables.
// It's possible to do this with Runtime.getRuntime().exec(), but it's not recommended due to security risks.